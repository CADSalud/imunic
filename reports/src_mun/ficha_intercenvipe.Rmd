---
output: pdf_document
linestretch: 1.1
header-includes:
  - \usepackage{color}
  - \usepackage[spanish]{babel}
  - \usepackage[utf8]{inputenc}
  - \usepackage{amsmath,amsfonts,amsthm}
  - \usepackage[svgnames]{xcolor}
  - \renewcommand{\familydefault}{\sfdefault}
  - \usepackage{fancyhdr}
  - \pagestyle{fancy}
  - \lhead{Ficha contextual}
  - \chead{}
  - \rhead{2018}
  - \lfoot{\thepage}
  - \cfoot{}
  - \rfoot{\includegraphics[width=1.5cm]{logo-CAD.png}} 
---



```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
library(tidyverse)

# Municipios
source("run_reports.R")

# municipios_selec <- c("Sultepec","Texcaltitlán", "Almoloya de Alquisiras")
# municipios_selec <- c("Álvaro Obregón", 9)
# municipios_selec <- c("Sultepec", 15)
municipios_selec <- c("Texcaltitlán", 15)
tablas_l <- tablas_load_fun(municipios_selec = municipios_selec[1], 
                            state_code_selec = municipios_selec[2])
format_fun <- function(vector){
  format(vector, big.mark=",",scientific=FALSE)  
}
```


# Ficha contextual de datos para `r paste(tablas_l$entidad$NOM_MUN)` - `r paste(tablas_l$entidad$NOM_ENT)` 


## 1. Sociodemográficos

```{r}
tab_interc <- tablas_l$tab_interc
tab_estatal <- tablas_l$tab_estatal

pob_16a35 <- tab_interc$pob_15o35

pob_16a35_est <- tab_estatal$pob_15o35

jovenes_mun <- round(100*pob_16a35/tab_interc$pob_total)
jovenes_est <- round(100*pob_16a35_est/tab_estatal$pob_total)
comp_jovenes <- case_when(
  jovenes_mun>jovenes_est ~ "Mayor",
  jovenes_mun ==jovenes_est ~ "Igual",
  jovenes_mun<jovenes_est ~ "Menor"
)
```



1.1 Población general:

El municipio tiene una población de `r format_fun(tab_interc$pob_total)` personas. Donde el `r round(100*tab_interc$pob_mujeres/tab_interc$pob_total)`% son mujeres. Es decir, por cada hombre en el municipio hay `r round(tab_interc$pob_mujeres/tab_interc$pob_hombres, 1)` mujeres. 

Hay `r format_fun(pob_16a35)` jóvenes entre 15 y 35 años.
Esto representa el `r jovenes_mun`% de la población del municipio. `r paste(comp_jovenes)`
a la proporción en la entidad (`r jovenes_est`%).

```{r}
comp_ingreso <- case_when(
  tab_interc$ingreso_q50 >  tablas_l$tab_ingreso_ent$ingreso_q50 ~ "mayor",
  tab_interc$ingreso_q50 == tablas_l$tab_ingreso_ent$ingreso_q50 ~ "igual",
  tab_interc$ingreso_q50 <  tablas_l$tab_ingreso_ent$ingreso_q50 ~ "menor")
```

El 50% de la población gana \$`r format_fun(round(tab_interc$ingreso_q50))` o menos al mes. Esto es `r paste(comp_ingreso)` al valor estatal \$`r format_fun(round(tablas_l$tab_ingreso_ent$ingreso_q50))`.


La diferencia entre el ingreso por persona del 25% más bajo y el 75% más alto es de `r format_fun(round(tab_interc$ingreso_q75 - tab_interc$ingreso_q25))`. Esto representa aproximadamente `r round(tab_interc$ingreso_q75/tab_interc$ingreso_q25)` veces el nivel bajo en el alto. A nivel estatal es aproximadamente `r round(tablas_l$tab_ingreso_ent$ingreso_q75/tablas_l$tab_ingreso_ent$ingreso_q25)` veces. 


1.2 Población vulnerable: 

```{r}
porc75 <- round(100*tab_interc$pob_mas75/tab_interc$pob_total)
porc75est <- round(100*tab_estatal$pob_mas75/tab_estatal$pob_total)
comp_porc75 <- case_when(
  porc75 >  porc75est ~ "mayor",
  porc75 == porc75est ~ "igual",
  porc75 <  porc75est ~ "menor")
```


  - Hay `r format_fun(tab_interc$pob_mas75)` personas de 75 años o mas. Esto representa el `r porc75`% de la población del municipio. Esto es `r comp_porc75` que el `r porc75est`% observado en la entidad. Esta población representa el `r round(100*tab_interc$pob_mas75/tab_estatal$pob_mas75, 1)`% de los mayores de 75 años en el estado.
  
  - Hay `r format_fun(tab_interc$pob_perte_indigena)` pertenecientes a la cultura indígena. Esto es el `r round(100*tab_interc$pob_perte_indigena/tab_interc$pob_total)`% de la población del municipio.
  
  - Hay `r format_fun(tab_interc$pob_15mas_sinedubasica)` personas de 15 o más sin educación básica. Esto es el `r round(100*tab_interc$pob_15mas_sinedubasica/tab_interc$pob_15omas)`% de la población mayor de 15 años del municipio.
  
  - Hay `r format_fun(tab_interc$madres_solteras)` madres solteras en el municipio. Esto es el `r round(100*tab_interc$madres_solteras/(tab_interc$madres_solteras + tab_interc$madres_nosolteras))`% de las madres del municipio. 
  
  - Hay `r format_fun(tab_interc$amas_casa)` personas que se dedican a las labores del hogar sin recibir pago.

  - Hay `r format_fun(tab_interc$incapacitado)` personas incapacitadas física o mentalmente para trabajar.



## 2. Salud

2.1 Percepción de la población sobre problemáticas principales en el municipio:  

```{r}
tab_envipe <- tablas_l$tab_envipe
tab <- tab_envipe %>% 
  filter(Tema == "importancia", Descripción == "Salud")
if( is.na(tab$valor)){
  frase <- "Este municipio no tiene información sobre el ranking de salud como problemática principal"
}
if( !is.na(tab$valor)){
  frase <- paste("El", paste0(round(100*tab$valor), "%"), "considera el tema salud como una de las tres principales problemáticas en el municipio")
}
```


`r paste(frase)`.
  

2.2 Afiliación a servicios de salud:

  - El `r round(100*tab_interc$ind_prop_seguroimss)`% esta afiliado al IMSS.
  
  - El `r round(100*tab_interc$ind_prop_seguropopu)`% esta afiliado al Seguro Popular. 
  
  - El `r round(100*tab_interc$ind_prop_seguropriv)`% cuenta con seguro privado. 
  
  
  
  

## 3. Seguridad

3.1 Percepción de la población sobre problemáticas principales en el municipio:  

```{r}
tab <- tab_envipe %>% 
  filter(Tema == "importancia", Descripción %in% c( "Narco", "Inseguridad", "Impunidad"))
if( sum(is.na(tab$valor)) == 3 ) {
  frase <- "Este municipio no tiene información sobre el ranking de problemáticas de seguridad"
}
if( sum(is.na(tab$valor)) < 3 ){
  
  tt <- tab %>% 
    filter(!is.na(valor)) %>% 
    mutate(valor = round(100*valor)) %>% 
    arrange(desc(valor))
  
  frase <- paste("En el municipio el", 
        paste0(paste0(tt$valor, "% considera "), tolower(tt$Descripción), collapse = ", " ), 
        "entre las tres problemáticas principales")
}
```


```{r}
tab <- tab_envipe %>% 
  filter(Tema == "inseguridad")
frase_inseg <- ""
if( !is.na(tab$valor)){
  frase_inseg <- paste0("El ", round(tab$valor*100), "% considera su municipio inseguro.")
}
```

`r paste(frase, frase_inseg, sep = ". ")`



3.2 Problemáticas locales alrededor de la vivienda: 

  - Invasión de predios
  - Consumo de drogas
  - Robos/Asaltos frecuentes
  - Venta de droga
  - Disparos frecuentes
  - Secuestros
  - Homicidios
  - Extorsiones/Cobro de pisos
  - Enfrentamientos de policías con ciudadanos

## 4. Economía

4.1 Percepción de la población sobre problemáticas principales en el municipio:  

```{r}
tab <- tab_envipe %>% 
  filter(Tema == "importancia", Descripción == "Precios")
if( is.na(tab$valor)){
  frase <- "Este municipio no tiene información sobre el ranking de aumento de precios como problemática principal"
}
if( !is.na(tab$valor)){
  frase <- paste("El", paste0(round(100*tab$valor), "%"), "considera el aumento de precios como una de las tres principales problemáticas en el municipio")
}
```

`r frase`.



4.2 Gasto trimestral promedio por concepto:

  - Salud (DAVID: src/indicadores_enigh.R   enigh2016mun::salud_prom)
  - Educación (DAVID: src/indicadores_enigh.R   enigh2016mun::educ_prom)

4.3 Ingreso trimetral del hogar promedio (DAVID: src/indicadores_enigh.R   enigh2016mun::ingr_prom)

## 5. Educación

5.1 Percepción de la población sobre problemáticas principales en el municipio:  

```{r}
tab <- tab_envipe %>% 
  filter(Tema == "importancia", Descripción == "Educación")
if( is.na(tab$valor)){
  frase <- "Este municipio no tiene información sobre el ranking de educación como problemática principal"
}
if( !is.na(tab$valor)){
  frase <- paste("El", paste0(round(100*tab$valor), "%"), "considera educación como una de las tres principales problemáticas en el municipio")
}
```

`r frase`.


5.2 Proporción de jóvenes en edad de asisitir a la escuela:

  - Preescolar
  - Primario
  - Secundario
  - Preparatoria
  - Universaidad

## 6. Desarrollo Social

6.1 Percepción de la población sobre problemáticas principales en el municipio:  


```{r}
tab <- tab_envipe %>% 
  filter(Tema == "importancia", Descripción %in% c( "Agua", "Pobreza"))
if( sum(is.na(tab$valor)) == 2 ) {
  frase <- "Este municipio no tiene información sobre el ranking de problemáticas de seguridad"
}
if( sum(is.na(tab$valor)) < 2 ){
  
  tt <- tab %>% 
    filter(!is.na(valor)) %>% 
    mutate(valor = round(100*valor)) %>% 
    arrange(desc(valor))
  
  frase <- paste("En el municipio el", 
        paste0(paste0(tt$valor, "% considera "), tolower(tt$Descripción), collapse = " y " ), 
        "entre las tres problemáticas principales")
}
```

`r frase`.


6.2 Problemas de la colonia

  - Baches o fugas de agua
  - Falta de alumbrado público

6.3 Infraestructura en servicios básicos (porcentaje de viviendas)

  - Agua potable (DAVID: damus/bd_imunac_login::sb_agua_potable)
  - Almacenamiento de agua (DAVID: damus/bd_imunac_login::sb_almacenamiento_agua)
  - Electricidad (DAVID: damus/bd_imunac_login::sb_electricidad)
  - Combustible cocina (DAVID: damus/bd_imunac_login::sb_combustible_cocina)
  - Material vivienda (DAVID: damus/bd_imunac_login::sb_material_vivienda)
  - Saneamiento (DAVID: damus/bd_imunac_login::sb_saneamiento)
  - Manejo de residuos (DAVID: damus/bd_imunac_login::sb_manejo_residuos)
  - Telecomunicaciones (DAVID: damus/bd_imunac_login::sb_telecomunicaciones)
  - Espacio suficiente (DAVID: damus/bd_imunac_login::sb_espacio)

6.4 Porcentaje de hogares por encima de la línea de bienestar mínima (DAVID: src/indicadores_enigh.R   enigh2016mun::phog_arriba_lbm)

## 7. Trabajo

7.1 Percepción de la población sobre problemáticas principales en el municipio:  
  
```{r}
tab <- tab_envipe %>% 
  filter(Tema == "importancia", Descripción == "Desempleo")
if( is.na(tab$valor)){
  frase <- "Este municipio no tiene información sobre el ranking de desempleo como problemática principal"
}
if( !is.na(tab$valor)){
  frase <- paste("El", paste0(round(100*tab$valor), "%"), "considera desempleo como una de las tres principales problemáticas en el municipio")
}
```

`r frase`.
  
7.2 Número de empleos por cada habitante en el municipio  (DAVID: damus/bd_imunac_login::dt_empleo_uso_suelo)